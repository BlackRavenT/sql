<Window x:Class="ViewEmpl.EmplView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ViewEmpl"
        mc:Ignorable="d"
        Title="Сотрудники" Height="350" Width="525">
	<Window.DataContext>
	 	<Binding Source="ViewModel.ViewModelEmpl"></Binding>
	</Window.DataContext>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="25"> </RowDefinition>
			<RowDefinition></RowDefinition>
		</Grid.RowDefinitions>
		<WrapPanel Grid.Row="0" ItemWidth="70">
			<Button Background="AliceBlue" Content="Добавить"/>
			<Button Background="AliceBlue" Content="Обновить" Margin="1 0 1 0"/>
			<Button Background="AliceBlue" Content="Удалить"/>
		</WrapPanel>
		<DataGrid x:Name="EmplGrid" Grid.Row="1" AutoGenerateColumns="False" ItemsSource="{Binding Employees}">
			<DataGrid.Columns>
				<DataGridTextColumn Header="ФИО" Binding="{Binding EmplName}" Width="*"/>
				<DataGridTextColumn Header="Департамент" Binding="{Binding Department}" Width="100"/>
				<DataGridTextColumn Header="Степень" Binding="{Binding ScienceDegree}" Width="150"/>
				<DataGridTextColumn Header="Нагрузка, ч" Binding="{Binding Hours}" Width="70"/>
			</DataGrid.Columns>
		</DataGrid>
		
	</Grid>
</Window>
